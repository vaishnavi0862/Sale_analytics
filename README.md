# Sale_analytics
Built an end-to-end sales analytics project that generates Excel source data, loads it into a SQLite star-schema using Python, runs SQL analytics queries, exports a dashboard-friendly dataset, and visualizes results in Tableau (revenue trend, top products, regional/segment performance, and customer ranking).
